---
title: v5.8 — 货币感知的 IGNORED 摘要 + run_supplemental 静默分支
type: feat
status: active
date: 2026-05-03
origin: docs/brainstorms/2026-05-03-v58-currency-summary-silent-retry-requirements.md
---

# v5.8 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v5.8 with three independent fixes: OCR schema knows currency (fixes USD IGNORED shown as ¥), chat summary's IGNORED line carries totals + top-sender + reply CTA, and `run_supplemental` on the initial run emits zero user-facing output — only the supplemental run delivers files to the user.

**Architecture:**
- **Unit A (数据层 + MD 呈现层)**: `scripts/core/prompts.py` 新增 `currency` 字段（ISO-4217，默认 `"CNY"`）；`scripts/core/llm_ocr.py` 在 `extract_from_bytes` 出口对缺失的 `currency` 回填 `"CNY"`（老 cache 兼容）；`scripts/postprocess.py` 顶层加 `_CURRENCY_SYMBOLS` + `currency_symbol()` 查表函数；`scripts/download-invoices.py::write_report_md` 里的 §已忽略 渲染从硬编码 `¥` 切到 `currency_symbol()`。
- **Unit B (摘要呈现层)**: `scripts/postprocess.py::print_openclaw_summary` 参数从 `ignored_count: int = 0` 改成 `ignored_records: list[dict] | None = None`；新增 `_ignored_summary()` helper；当前那一行（1230-1234）换成"2 行：`📭 已忽略 N 张...（total，主要来自 domain）` + `回复 "加 domain" ...`"。
- **Unit C (合约层)**: `scripts/download-invoices.py` 两个 `print_openclaw_summary` 调用点前加静默分支 —— `not args.supplemental and missing_status == "run_supplemental"` → stderr 打 `AGENT_HINT: run_supplemental ...` + `sys.exit(EXIT_PARTIAL)`，不发任何哨兵；`SKILL.md` 更新 §Exit Codes / §Presenting Results / §Agent Playbook / §Invariants / §Loop Playbook + 新增 §User Reply Conventions + §Lessons Learned v5.8。

**Tech Stack:** Python 3.10+, pytest, pdftotext (validation.py), boto3 / anthropic / openai SDKs (OCR provider-agnostic).

---

## File Structure

### 修改

| 文件 | 修改范围 | Unit |
|---|---|---|
| `scripts/core/prompts.py` | `get_ocr_prompt()` 通用字段表插入 `currency` 行；3 段 example JSON（电子发票、酒店水单、网约车）各加 `"currency": "CNY"` | A |
| `scripts/core/llm_ocr.py` | `extract_from_bytes()` 返回前 defensive fill: `data.setdefault("currency", "CNY")`（含老 cache 命中路径） | A |
| `scripts/postprocess.py` | 顶层加 `_CURRENCY_SYMBOLS` dict + `currency_symbol()` helper；加 `_ignored_summary()` helper；`print_openclaw_summary` 签名 `ignored_count: int = 0` → `ignored_records: list[dict] \| None = None`；末尾 IGNORED 渲染改成 2 行 | A, B |
| `scripts/download-invoices.py` | 顶部 `from postprocess import currency_symbol` 导入；`write_report_md` 里 §IGNORED 渲染（当前 754-757）切 `currency_symbol()`；两个 `print_openclaw_summary` 调用点参数从 `ignored_count=len(...)` 改成 `ignored_records=...`；两个调用点**前**加静默分支（`run_supplemental` → stderr + exit 5，跳过哨兵） | A, B, C |
| `SKILL.md` | line 1 版本号 → `v5.8`；§Exit Codes 表 exit=5 注解扩充；§Presenting Results / §Agent Playbook 加守则 0（sentinel absence is valid）；§Invariants 松绑；§Loop Playbook 补一句；新增 §User Reply Conventions；§Lessons Learned 新增 `🟢 v5.8` 节 | C |
| `CHANGELOG.md` | 新增 `## v5.8` 条目（最上面），按既有格式写 feat/feat/feat + 测试数 + Agent 合约变更说明 | C（收尾） |
| `tests/test_postprocess.py` | 新增 `TestCurrencySymbolTable` / `TestIgnoredSummaryHelper` / `TestIgnoredMdRenderingCurrency`；扩 `TestPrintOpenClawSummary`（改签名 + 新 IGNORED 行断言） | A, B |
| `tests/test_agent_contract.py` | 新增 `TestRunSupplementalSilence`；`TestChatSentinelContract` 已有的"每次运行都出哨兵"测试在 `run_supplemental` 条件下改断言 | C |

### Not modified

- `scripts/core/matching.py`（不触）
- `scripts/core/classify.py`（不触）
- `scripts/invoice_helpers.py`（不触）
- `missing.json` schema（1.0 不变）

---

## Requirements Trace

- **R1 (Unit A).** OCR schema 新增 `currency` 字段；`llm_ocr.extract_from_bytes` 缺失字段回填 `"CNY"`；老 cache 命中仍回填。
- **R2 (Unit A).** `currency_symbol()` 查表放 `postprocess.py` 顶层；CNY→¥ / USD→$ / EUR→€ / GBP→£ / JPY→¥ / HKD→HK$；未知码 fallback `"{CODE} "`。
- **R3 (Unit A).** `write_report_md` §IGNORED 渲染从硬编 `¥` 切 `currency_symbol()`。
- **R4 (Unit B).** `print_openclaw_summary` 参数 `ignored_count: int` → `ignored_records: list[dict] | None`；`_ignored_summary()` 产 `(totals-by-currency, top-domain, top-count)`；渲染 2 行 "总金额 + 主 sender + CTA"；`top_domain == "未知发件人"` 时省 CTA。
- **R5 (Unit C).** `not args.supplemental and missing_status == "run_supplemental"` → stderr `AGENT_HINT: run_supplemental ...` + `sys.exit(EXIT_PARTIAL)` + 跳过 `print_openclaw_summary`。其他三种 missing_status 和 `args.supplemental=True` 的补搜照常发哨兵。
- **R6 (Unit C).** `SKILL.md` 5 节改 + 1 节新增（§User Reply Conventions） + Lessons Learned 新增 v5.8。

---

## Scope Boundaries

- **不** 改 aggregation 支持多币种（reimbursable 100% CNY，IGNORED 不进 aggregation）。
- **不** 改 CSV 币种（CSV 无 IGNORED 行）。
- **不** 改 `missing.json` schema（1.0 不变；不加 `ignored_count` 顶层）。
- **不** 加 exit code 6（复用 exit 5 + stderr `AGENT_HINT:` 区分）。
- **不** 主动清 `~/.cache/gmail-invoice-downloader/ocr/`。
- **不** 让 Skill 自动写 `learned_exclusions.json`。
- **不** 在静默分支产出"预览 MD / 草稿 CSV"（硬盘产物就是原版，供补搜 merge）。
- **不** 在 Skill 里加 "占位消息" 逻辑（Agent 职责，SKILL.md 建议但不强制）。

---

## Unit 执行顺序

```
Unit A (currency schema + symbol table + MD fix) [依赖链底]
  └─ Unit B (summary line E)                     [依赖 A 的 currency_symbol]
Unit C (silent run_supplemental + SKILL.md)      [独立于 A/B，但 commit 顺序放最后]
  └─ Unit D (chore: SKILL.md version + CHANGELOG) [收尾]
```

推荐 commit 顺序：A → B → C → D。feature 分支 `feat/v5.8-currency-summary-silent-retry`，单 PR。

---

# Unit A — OCR schema 加 `currency` + MD §IGNORED 切符号查表

## Task A1: OCR prompt 加 `currency` 字段

**Files:**
- Modify: `scripts/core/prompts.py:17-27`（通用字段表）
- Modify: `scripts/core/prompts.py:160-200`（3 段 example JSON）
- Test: `tests/test_postprocess.py`（扩 `TestPromptContract`，或新增 `TestCurrencyPromptContract`）

- [ ] **Step 1: 写失败测试 — prompt 包含 currency 字段定义和 example**

在 `tests/test_postprocess.py` 文件末尾加：

```python
class TestCurrencyPromptContract:
    """Unit A: OCR prompt must declare `currency` field and example values."""

    def test_prompt_declares_currency_field(self):
        from core.prompts import get_ocr_prompt
        prompt = get_ocr_prompt()
        assert "| currency |" in prompt, "currency 行必须在通用字段表"
        assert "ISO-4217" in prompt or "ISO 4217" in prompt, \
            "说明必须引用 ISO-4217 三字母码标准"
        assert "CNY" in prompt and "USD" in prompt, \
            "示例币种必须覆盖 CNY / USD"

    def test_prompt_examples_include_cny_default(self):
        from core.prompts import get_ocr_prompt
        prompt = get_ocr_prompt()
        # 3 段 example JSON 至少各有一处 "currency": "CNY"
        assert prompt.count('"currency": "CNY"') >= 3, \
            "电子发票 / 酒店水单 / 网约车 3 段 example 都要列 currency=CNY"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /Users/kentpeng/projects/gmail-invoice-downloader
python3 -m pytest tests/test_postprocess.py::TestCurrencyPromptContract -v
```

Expected: 2 FAIL，`"| currency |"` 不在 prompt 里。

- [ ] **Step 3: 修 prompt 通用字段表**

打开 `scripts/core/prompts.py`，在第 27 行 `| isChineseInvoice | boolean | ... |` 之后插入一行：

```
| currency | string | ISO-4217 三字母币种代码。大陆增值税发票/普票/电子发票 = "CNY"；美元账单 = "USD"；欧元 = "EUR"；未写明或识别不出 → 返回 "CNY"（保守默认，避免误改人民币发票）|
```

- [ ] **Step 4: 在 3 段 example JSON 显式列 currency**

仍在 `scripts/core/prompts.py`：

电子发票 example（当前第 160-172 行）— 在 `"isChineseInvoice": true` 上一行加：
```
  "currency": "CNY",
```

酒店水单 example（当前第 177-187 行）— 在 `"confirmationNo": "..."` 后加：
```
,
  "currency": "CNY"
```
（注意该 JSON 块用 `confirmationNo` 做最后一行，所以要把末尾 `}` 前先补逗号。完成后 JSON 结构合法。）

网约车 example（当前第 191-200 行）— 在 `"city": "南京"` 后同上加 `"currency": "CNY"`。

完成后，每段 example 里应出现一次 `"currency": "CNY"`，累计 3 次。

- [ ] **Step 5: 跑测试确认通过**

```bash
python3 -m pytest tests/test_postprocess.py::TestCurrencyPromptContract -v
```

Expected: 2 PASS。

- [ ] **Step 6: 跑全 test_postprocess 确保无回归**

```bash
python3 -m pytest tests/test_postprocess.py -q
```

Expected: 全绿（prompt 对既有 `TestPromptContract` 无字段删除，只加字段）。

- [ ] **Step 7: Commit**

```bash
cd /Users/kentpeng/projects/gmail-invoice-downloader
git checkout -b feat/v5.8-currency-summary-silent-retry
git add scripts/core/prompts.py tests/test_postprocess.py
git commit -m "$(cat <<'EOF'
feat(prompts): currency field in OCR schema (v5.8 Unit A.1)

Add `currency` field (ISO-4217 three-letter code, default "CNY") to
the general field table and to all three example JSON blocks (电子发票
/ 酒店水单 / 网约车). Unblocks downstream USD/EUR/etc rendering in the
§IGNORED 非报销票据 section.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task A2: `llm_ocr.extract_from_bytes` defensive currency fill

**Files:**
- Modify: `scripts/core/llm_ocr.py:213-233`（`extract_from_bytes` 出口）
- Test: `tests/test_postprocess.py`（新增 `TestCurrencyDefensiveFill`）

- [ ] **Step 1: 写失败测试 — 缺 `currency` 的 LLM 响应被回填 "CNY"；老 cache 命中也回填**

在 `tests/test_postprocess.py` 文件末尾加：

```python
class TestCurrencyDefensiveFill:
    """Unit A: extract_from_bytes guarantees currency field presence."""

    def test_missing_currency_defaults_to_cny(self, tmp_path, monkeypatch):
        from core import llm_ocr

        # Stub extract_with_retry to return LLM output without `currency`.
        monkeypatch.setattr(
            llm_ocr, "extract_with_retry",
            lambda pdf, prompt, client: '{"transactionAmount": 100.0, "vendorName": "T"}',
        )
        # Stub get_client to bypass provider init.
        class _FakeClient:
            provider_name = "anthropic"
        monkeypatch.setattr(llm_ocr, "get_client", lambda: _FakeClient())

        result = llm_ocr.extract_from_bytes(
            b"%PDF-fake", use_cache=False, cache_dir=tmp_path,
        )
        assert result.get("currency") == "CNY"

    def test_empty_currency_is_normalized_to_cny(self, tmp_path, monkeypatch):
        from core import llm_ocr
        monkeypatch.setattr(
            llm_ocr, "extract_with_retry",
            lambda pdf, prompt, client: '{"transactionAmount": 1, "currency": ""}',
        )
        class _FakeClient:
            provider_name = "anthropic"
        monkeypatch.setattr(llm_ocr, "get_client", lambda: _FakeClient())

        result = llm_ocr.extract_from_bytes(
            b"%PDF-fake2", use_cache=False, cache_dir=tmp_path,
        )
        assert result.get("currency") == "CNY"

    def test_old_cache_without_currency_gets_filled(self, tmp_path):
        from core import llm_ocr
        import json, hashlib

        pdf_bytes = b"%PDF-old-cache"
        # Seed a cache entry that predates v5.8 (no `currency` field).
        tmp_path.mkdir(exist_ok=True)
        digest = hashlib.sha256(pdf_bytes).hexdigest()[:16]
        cache_file = tmp_path / f"{digest}.json"
        cache_file.write_text(json.dumps({
            "ocr": {"transactionAmount": 50.0, "vendorName": "legacy"},
            "schema_version": "1.0",
        }), encoding="utf-8")

        result = llm_ocr.extract_from_bytes(
            pdf_bytes, use_cache=True, cache_dir=tmp_path,
        )
        assert result.get("currency") == "CNY"

    def test_explicit_usd_is_preserved(self, tmp_path, monkeypatch):
        from core import llm_ocr
        monkeypatch.setattr(
            llm_ocr, "extract_with_retry",
            lambda pdf, prompt, client: '{"transactionAmount": 10, "currency": "USD"}',
        )
        class _FakeClient:
            provider_name = "anthropic"
        monkeypatch.setattr(llm_ocr, "get_client", lambda: _FakeClient())

        result = llm_ocr.extract_from_bytes(
            b"%PDF-usd", use_cache=False, cache_dir=tmp_path,
        )
        assert result.get("currency") == "USD"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tests/test_postprocess.py::TestCurrencyDefensiveFill -v
```

Expected: 4 FAIL（`currency` 字段缺失或空串时不会自动变 `"CNY"`）。

- [ ] **Step 3: 实现 defensive fill**

打开 `scripts/core/llm_ocr.py`。找到第 213-233 行的 `extract_from_bytes` 函数。

在 `_cache_read` 返回之后（第 217-218 行那段 `if cached is not None: return cached` 之前）不动 —— cache hit 的正确做法是**在函数最末尾 return 之前**统一兜底。

把函数末尾改成下面这样（替换第 215-233 行）：

```python
    cache_dir = cache_dir or DEFAULT_CACHE_DIR

    if use_cache:
        cached = _cache_read(pdf_bytes, cache_dir)
        if cached is not None:
            # v5.8 Unit A.2: pre-v5.8 caches lack `currency`; fill defensively
            # so downstream renderers (MD §IGNORED, summary line) can trust
            # the field exists.
            if not cached.get("currency"):
                cached["currency"] = "CNY"
            return cached

    client = llm_client or get_client()
    if getattr(client, "provider_name", "") == "none":
        raise LLMDisabledError("LLM extraction skipped (provider=none)")

    prompt = get_ocr_prompt()
    response_text = extract_with_retry(pdf_bytes, prompt, client=client)

    data = parse_llm_response(response_text)
    data = validate_and_fix_vendor_info(data)

    # v5.8 Unit A.2: LLM may omit currency despite prompt; guarantee presence.
    if not data.get("currency"):
        data["currency"] = "CNY"

    if use_cache:
        _cache_write(pdf_bytes, data, cache_dir)

    return data
```

- [ ] **Step 4: 跑测试确认通过**

```bash
python3 -m pytest tests/test_postprocess.py::TestCurrencyDefensiveFill -v
```

Expected: 4 PASS.

- [ ] **Step 5: 跑全 llm_ocr 相关测试避免回归**

```bash
python3 -m pytest tests/test_postprocess.py::TestOCRCache tests/test_postprocess.py::TestProviderMatrix -q
```

Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add scripts/core/llm_ocr.py tests/test_postprocess.py
git commit -m "$(cat <<'EOF'
feat(llm_ocr): defensive fill currency=CNY (v5.8 Unit A.2)

Guarantee `ocr["currency"]` presence at extract_from_bytes exit for both
fresh LLM calls (may omit the field despite prompt) and cache hits
(pre-v5.8 entries lack the field entirely). Default to "CNY" for
backward-compatible rendering of legacy caches.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task A3: `currency_symbol()` 查表函数

**Files:**
- Modify: `scripts/postprocess.py`（顶层工具区，约在 `CATEGORY_LABELS` 之前）
- Test: `tests/test_postprocess.py`（新增 `TestCurrencySymbolTable`）

- [ ] **Step 1: 写失败测试 — 查表覆盖 CNY/USD/EUR/未知/None/小写**

在 `tests/test_postprocess.py` 文件末尾加：

```python
class TestCurrencySymbolTable:
    """Unit A: currency_symbol maps ISO-4217 codes to display symbols.

    Contract:
      - Known codes → short symbol (¥/$/€/£ etc.)
      - Unknown codes → "{CODE} " (uppercase + trailing space)
      - None / empty → ¥ (CNY fallback)
      - Case-insensitive input
    """

    def test_known_codes(self):
        from postprocess import currency_symbol
        assert currency_symbol("CNY") == "¥"
        assert currency_symbol("USD") == "$"
        assert currency_symbol("EUR") == "€"
        assert currency_symbol("GBP") == "£"
        assert currency_symbol("JPY") == "¥"
        assert currency_symbol("HKD") == "HK$"

    def test_unknown_code_preserves_as_prefix(self):
        from postprocess import currency_symbol
        # Hallucinated / malformed code: fallback to uppercase + space
        assert currency_symbol("RMB") == "RMB "
        assert currency_symbol("XYZ") == "XYZ "

    def test_none_is_cny(self):
        from postprocess import currency_symbol
        assert currency_symbol(None) == "¥"

    def test_empty_string_is_cny(self):
        from postprocess import currency_symbol
        assert currency_symbol("") == "¥"

    def test_lowercase_input(self):
        from postprocess import currency_symbol
        assert currency_symbol("usd") == "$"
        assert currency_symbol("cny") == "¥"
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tests/test_postprocess.py::TestCurrencySymbolTable -v
```

Expected: 5 FAIL，`currency_symbol` 不存在。

- [ ] **Step 3: 实现 currency_symbol**

打开 `scripts/postprocess.py`。在文件头部 imports 之后、第 60 行 `# ====...==== \n# \n# ====...====` 那一段之前，找到一处现有的 module-level 常量区（`_MISSING_STATUSES` 附近也行，但那个在 1101 行偏后）。

最合适的位置：第 60 行那段注释横栏**前面**紧邻一行（约第 58-60 行），也就是 imports 和 `CATEGORY_LABELS` 之间。插入：

```python
# =============================================================================
# Currency symbols (v5.8 Unit A.3)
# =============================================================================
# Maps ISO-4217 three-letter codes to display prefixes used in §IGNORED MD
# and the OpenClaw chat summary's IGNORED line. Unknown codes fall back to
# "{CODE} " so weird LLM output never silently becomes ¥.

_CURRENCY_SYMBOLS = {
    "CNY": "¥",
    "USD": "$",
    "EUR": "€",
    "GBP": "£",
    "JPY": "¥",
    "HKD": "HK$",
}


def currency_symbol(code) -> str:
    """Return the display prefix for an ISO-4217 code.

    None / empty / lowercase are all normalized. Unknown codes return
    `"{UPPER} "` (trailing space preserved so the caller can concatenate
    directly with the amount).
    """
    key = (code or "CNY").upper()
    if key in _CURRENCY_SYMBOLS:
        return _CURRENCY_SYMBOLS[key]
    return key + " "


```

- [ ] **Step 4: 跑测试确认通过**

```bash
python3 -m pytest tests/test_postprocess.py::TestCurrencySymbolTable -v
```

Expected: 5 PASS.

- [ ] **Step 5: Commit**

```bash
git add scripts/postprocess.py tests/test_postprocess.py
git commit -m "$(cat <<'EOF'
feat(postprocess): currency_symbol() lookup table (v5.8 Unit A.3)

Module-level _CURRENCY_SYMBOLS dict + currency_symbol() helper covering
CNY/USD/EUR/GBP/JPY/HKD. Unknown codes fall back to "{CODE} " (uppercase
+ space) so malformed LLM output never silently collapses to ¥. Consumed
by the §IGNORED MD renderer (Unit A.4) and the OpenClaw summary line
(Unit B).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task A4: MD §IGNORED 渲染切 `currency_symbol`

**Files:**
- Modify: `scripts/download-invoices.py:745-760`（`write_report_md` 里 IGNORED 明细循环）
- Modify: `scripts/download-invoices.py`（顶部 imports）
- Test: `tests/test_postprocess.py`（新增 `TestIgnoredMdRenderingCurrency`）

- [ ] **Step 1: 写失败测试 — USD 记录 MD 渲染出 `$10.00`，不含 `¥`**

在 `tests/test_postprocess.py` 文件末尾加：

```python
class TestIgnoredMdRenderingCurrency:
    """Unit A.4: §IGNORED MD renders the correct currency symbol per record."""

    @staticmethod
    def _invoke_md(tmp_path, ignored_records):
        """Invoke write_report_md with minimal aggregation so we can read back
        the §IGNORED section without touching the matching pipeline."""
        # write_report_md is in scripts/download-invoices.py (hyphenated). Import
        # via importlib to sidestep the filename hyphen.
        import importlib.util, os, sys, pathlib
        root = pathlib.Path(__file__).parent.parent
        spec = importlib.util.spec_from_file_location(
            "dl_cli",
            root / "scripts" / "download-invoices.py",
        )
        mod = importlib.util.module_from_spec(spec)
        sys.modules["dl_cli"] = mod
        spec.loader.exec_module(mod)

        from postprocess import do_all_matching, build_aggregation
        matching = do_all_matching([])
        agg = build_aggregation(matching, [])
        md_path = str(tmp_path / "report.md")
        mod.write_report_md(
            md_path,
            downloaded_all=[], failed=[], skipped=[],
            matching_result=matching,
            date_range=("2026/05/01", "2026/05/02"),
            iteration=1,
            supplemental=False,
            aggregation=agg,
            out_of_range_items=[],
            ignored_records=ignored_records,
        )
        return pathlib.Path(md_path).read_text(encoding="utf-8")

    def test_usd_record_renders_dollar_sign(self, tmp_path):
        records = [{
            "sender_email": "invoice+statements@mail.anthropic.com",
            "ocr": {"transactionAmount": 10.00, "currency": "USD"},
        }]
        md = self._invoke_md(tmp_path, records)
        assert "$10.00" in md
        assert "¥10.00" not in md

    def test_cny_record_renders_yuan(self, tmp_path):
        records = [{
            "sender_email": "billing@termius.com",
            "ocr": {"transactionAmount": 99.00, "currency": "CNY"},
        }]
        md = self._invoke_md(tmp_path, records)
        assert "¥99.00" in md
        assert "$99.00" not in md

    def test_missing_currency_defaults_to_yuan(self, tmp_path):
        records = [{
            "sender_email": "x@example.com",
            "ocr": {"transactionAmount": 50.00},  # no currency key
        }]
        md = self._invoke_md(tmp_path, records)
        assert "¥50.00" in md
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tests/test_postprocess.py::TestIgnoredMdRenderingCurrency -v
```

Expected: 第一个 USD 测试 FAIL (`$10.00` not found，当前渲染仍出 `¥10.00 USD`)。第二/三个可能意外 PASS，但 USD 的必 FAIL。

- [ ] **Step 3: 在 `download-invoices.py` 顶部加 `currency_symbol` 导入**

打开 `scripts/download-invoices.py`。找到顶部 import 区域（约第 70-85 行有一整块 `from postprocess import ...`）。给现有的 `from postprocess import (...)` 块里加一行：

```python
    currency_symbol,
```

保持字母序 / 按现有排序风格。如果块里已经有 `build_aggregation, merge_supplemental_downloads, print_openclaw_summary` 这些名字，`currency_symbol` 按字母顺序插在合适位置。

- [ ] **Step 4: 改 §IGNORED MD 渲染逻辑**

仍在 `scripts/download-invoices.py`，定位到第 745-760 行（`for rec in ignored_records:` 那个循环）。把里面的渲染部分：

```python
        for rec in ignored_records:
            sender_email = rec.get("sender_email") or ""
            label = sender_email if sender_email else "未知发件人"
            ocr = rec.get("ocr") or {}
            raw_amount = ocr.get("transactionAmount")
            try:
                amount = float(raw_amount) if raw_amount not in (None, "") else None
            except (TypeError, ValueError):
                amount = None
            currency = ocr.get("currency") or ""
            if amount is not None:
                prefix = "¥" if not currency or currency == "CNY" else ""
                suffix = f" {currency}" if currency and currency != "CNY" else ""
                lines.append(f"- {label}：{prefix}{amount:.2f}{suffix}")
            else:
                lines.append(f"- {label}：金额未识别")
        lines.append("")
```

替换为：

```python
        for rec in ignored_records:
            sender_email = rec.get("sender_email") or ""
            label = sender_email if sender_email else "未知发件人"
            ocr = rec.get("ocr") or {}
            raw_amount = ocr.get("transactionAmount")
            try:
                amount = float(raw_amount) if raw_amount not in (None, "") else None
            except (TypeError, ValueError):
                amount = None
            if amount is not None:
                sym = currency_symbol(ocr.get("currency"))
                lines.append(f"- {label}：{sym}{amount:.2f}")
            else:
                lines.append(f"- {label}：金额未识别")
        lines.append("")
```

- [ ] **Step 5: 跑测试确认通过**

```bash
python3 -m pytest tests/test_postprocess.py::TestIgnoredMdRenderingCurrency -v
```

Expected: 3 PASS.

- [ ] **Step 6: 跑全套测试避免回归**

```bash
python3 -m pytest tests/ -q
```

Expected: 全绿（除了可能已有的与 IGNORED rendering 相关但不断言具体符号的测试 — 这些应继续绿）。若有红，停下排查。

- [ ] **Step 7: Commit**

```bash
git add scripts/download-invoices.py tests/test_postprocess.py
git commit -m "$(cat <<'EOF'
feat(report): §IGNORED uses currency_symbol() (v5.8 Unit A.4)

Replace hard-coded ¥ prefix + " {currency}" suffix with currency_symbol()
lookup so USD/EUR/GBP IGNORED rows render correctly (fixes 4 Anthropic
billing entries in Q1 2025 batch that showed as ¥10.00 instead of $10.00).
Rendering keeps the "金额未识别" fallback for unconvertible amounts.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Unit B — 摘要 IGNORED 行扩成 "总金额 + 主 sender + CTA"

## Task B1: `_ignored_summary()` helper

**Files:**
- Modify: `scripts/postprocess.py`（工具区，在 `currency_symbol` 定义之后）
- Test: `tests/test_postprocess.py`（新增 `TestIgnoredSummaryHelper`）

- [ ] **Step 1: 写失败测试 — helper 聚合 totals-by-currency + top domain**

在 `tests/test_postprocess.py` 文件末尾加：

```python
class TestIgnoredSummaryHelper:
    """Unit B.1: _ignored_summary aggregates IGNORED records.

    Contract:
      - Returns (totals_by_currency: dict[str, float], top_domain: str, top_count: int)
      - Unconvertible amounts contribute 0.00 to their currency bucket
      - Missing sender_email → '未知发件人' bucket
      - Top domain = argmax of per-domain counts (dict insertion order on ties)
    """

    def test_single_sender_usd(self):
        from postprocess import _ignored_summary
        recs = [
            {"sender_email": "invoice+statements@mail.anthropic.com",
             "ocr": {"transactionAmount": 10.0, "currency": "USD"}},
            {"sender_email": "invoice+statements@mail.anthropic.com",
             "ocr": {"transactionAmount": 20.0, "currency": "USD"}},
        ]
        totals, top_domain, top_count = _ignored_summary(recs)
        assert totals == {"USD": 30.0}
        assert top_domain == "mail.anthropic.com"
        assert top_count == 2

    def test_mixed_currency(self):
        from postprocess import _ignored_summary
        recs = [
            {"sender_email": "a@foo.com",
             "ocr": {"transactionAmount": 10.0, "currency": "USD"}},
            {"sender_email": "b@foo.com",
             "ocr": {"transactionAmount": 5.0, "currency": "CNY"}},
        ]
        totals, _top, _n = _ignored_summary(recs)
        assert totals == {"USD": 10.0, "CNY": 5.0}

    def test_missing_sender_bucketed(self):
        from postprocess import _ignored_summary
        recs = [
            {"sender_email": "",
             "ocr": {"transactionAmount": 7.0, "currency": "CNY"}},
        ]
        _totals, top_domain, _n = _ignored_summary(recs)
        assert top_domain == "未知发件人"

    def test_unconvertible_amount_contributes_zero(self):
        from postprocess import _ignored_summary
        recs = [
            {"sender_email": "x@y.com",
             "ocr": {"transactionAmount": "N/A", "currency": "CNY"}},
        ]
        totals, _top, _n = _ignored_summary(recs)
        # Bucket exists at 0.0 — preserves currency visibility in summary output
        assert totals == {"CNY": 0.0}

    def test_missing_currency_buckets_as_cny(self):
        from postprocess import _ignored_summary
        recs = [
            {"sender_email": "x@y.com",
             "ocr": {"transactionAmount": 3.0}},  # no currency
        ]
        totals, _top, _n = _ignored_summary(recs)
        assert totals == {"CNY": 3.0}

    def test_top_domain_picks_most_frequent(self):
        from postprocess import _ignored_summary
        recs = [
            {"sender_email": "a@rare.com",
             "ocr": {"transactionAmount": 1.0, "currency": "CNY"}},
            {"sender_email": "a@common.com",
             "ocr": {"transactionAmount": 1.0, "currency": "CNY"}},
            {"sender_email": "b@common.com",
             "ocr": {"transactionAmount": 1.0, "currency": "CNY"}},
        ]
        _totals, top_domain, top_count = _ignored_summary(recs)
        assert top_domain == "common.com"
        assert top_count == 2
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tests/test_postprocess.py::TestIgnoredSummaryHelper -v
```

Expected: 6 FAIL，`_ignored_summary` 不存在。

- [ ] **Step 3: 实现 `_ignored_summary`**

打开 `scripts/postprocess.py`。在 `currency_symbol()` 函数定义之后（Task A3 插入的那块的末尾），再追加：

```python
def _ignored_summary(records):
    """Aggregate IGNORED records for the summary line.

    Returns:
        (totals_by_currency, top_domain, top_count)
        where totals_by_currency is dict[str, float], keyed by ISO-4217
        code (defaults to "CNY" if missing); unconvertible amounts contribute
        0.0 but still create the currency bucket so the symbol remains visible
        in the rendered line.

        top_domain is the most common sender email domain across records
        ('未知发件人' for entries without a valid from: header). Ties break
        by dict insertion order (first seen wins) — intentional YAGNI.

    This helper is pure (no I/O, no printing). The summary renderer
    (print_openclaw_summary) formats the tuple into display strings.
    """
    totals = {}
    domain_counts = {}
    for rec in records:
        ocr = rec.get("ocr") or {}
        raw_amt = ocr.get("transactionAmount")
        try:
            amt = float(raw_amt) if raw_amt not in (None, "") else 0.0
        except (TypeError, ValueError):
            amt = 0.0
        cur = (ocr.get("currency") or "CNY").upper()
        totals[cur] = totals.get(cur, 0.0) + amt

        sender = rec.get("sender_email") or ""
        if "@" in sender:
            domain = sender.split("@", 1)[-1]
        else:
            domain = "未知发件人"
        domain_counts[domain] = domain_counts.get(domain, 0) + 1

    top_domain, top_count = max(
        domain_counts.items(), key=lambda kv: kv[1],
    )
    return totals, top_domain, top_count
```

- [ ] **Step 4: 跑测试确认通过**

```bash
python3 -m pytest tests/test_postprocess.py::TestIgnoredSummaryHelper -v
```

Expected: 6 PASS.

- [ ] **Step 5: Commit**

```bash
git add scripts/postprocess.py tests/test_postprocess.py
git commit -m "$(cat <<'EOF'
feat(postprocess): _ignored_summary() helper (v5.8 Unit B.1)

Pure helper that aggregates IGNORED records into
(totals_by_currency, top_domain, top_count) for the chat-summary line.
Unconvertible amounts bucket at 0.0 (preserves currency symbol
visibility); missing sender_email → '未知发件人' (renderer uses this
as a sentinel to suppress the reply CTA).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task B2: 替换摘要 IGNORED 行 + 改 `print_openclaw_summary` 签名

**Files:**
- Modify: `scripts/postprocess.py:1116-1234`（`print_openclaw_summary` 签名 + IGNORED 行）
- Modify: `scripts/download-invoices.py:1091-1101`（call site 1）
- Modify: `scripts/download-invoices.py:1523-1534`（call site 2）
- Test: `tests/test_postprocess.py::TestPrintOpenClawSummary`（扩）

- [ ] **Step 1: 写失败测试 — 2 行摘要 + CTA + 省 CTA 边界**

在 `tests/test_postprocess.py::TestPrintOpenClawSummary` 类末尾（`test_zip_failure_degrades_gracefully` 之后、类结束之前）加：

```python
    # -- v5.8 Unit B: IGNORED summary line (totals + top sender + CTA) ----

    def _ignored_usd_records(self):
        return [
            {"sender_email": "invoice+statements@mail.anthropic.com",
             "ocr": {"transactionAmount": 10.0, "currency": "USD"}},
            {"sender_email": "invoice+statements@mail.anthropic.com",
             "ocr": {"transactionAmount": 20.0, "currency": "USD"}},
        ]

    def test_ignored_line_shows_total_and_top_sender(self, tmp_path):
        agg = self._populated_agg()
        lines = self._capture(
            aggregation=agg, **self._default_paths(tmp_path),
            missing_status="stop", date_range=("2026/04/01", "2026/04/30"),
            ignored_records=self._ignored_usd_records(),
        )
        text = "\n".join(lines)
        assert "📭 已忽略 2 张非报销票据" in text
        assert "$30.00" in text
        assert "主要来自 mail.anthropic.com" in text

    def test_ignored_line_shows_cta_with_domain(self, tmp_path):
        agg = self._populated_agg()
        lines = self._capture(
            aggregation=agg, **self._default_paths(tmp_path),
            missing_status="stop", date_range=("2026/04/01", "2026/04/30"),
            ignored_records=self._ignored_usd_records(),
        )
        text = "\n".join(lines)
        assert '加 mail.anthropic.com' in text
        assert "learned_exclusions.json" in text

    def test_ignored_line_mixed_currency_sorted(self, tmp_path):
        agg = self._populated_agg()
        recs = [
            {"sender_email": "a@foo.com",
             "ocr": {"transactionAmount": 5.0, "currency": "CNY"}},
            {"sender_email": "a@foo.com",
             "ocr": {"transactionAmount": 10.0, "currency": "USD"}},
        ]
        lines = self._capture(
            aggregation=agg, **self._default_paths(tmp_path),
            missing_status="stop", date_range=("2026/04/01", "2026/04/30"),
            ignored_records=recs,
        )
        text = "\n".join(lines)
        # Sorted alphabetically by currency code: CNY < USD
        assert "¥5.00 / $10.00" in text

    def test_ignored_line_absent_when_empty(self, tmp_path):
        agg = self._populated_agg()
        lines = self._capture(
            aggregation=agg, **self._default_paths(tmp_path),
            missing_status="stop", date_range=("2026/04/01", "2026/04/30"),
            ignored_records=[],
        )
        text = "\n".join(lines)
        assert "📭" not in text

    def test_ignored_line_absent_when_none(self, tmp_path):
        agg = self._populated_agg()
        lines = self._capture(
            aggregation=agg, **self._default_paths(tmp_path),
            missing_status="stop", date_range=("2026/04/01", "2026/04/30"),
            # No ignored_records kwarg at all → defaults to None
        )
        text = "\n".join(lines)
        assert "📭" not in text

    def test_ignored_line_unknown_sender_suppresses_cta(self, tmp_path):
        agg = self._populated_agg()
        recs = [
            {"sender_email": "",
             "ocr": {"transactionAmount": 1.0, "currency": "CNY"}},
        ]
        lines = self._capture(
            aggregation=agg, **self._default_paths(tmp_path),
            missing_status="stop", date_range=("2026/04/01", "2026/04/30"),
            ignored_records=recs,
        )
        text = "\n".join(lines)
        # First line still present (count + total), but no CTA reply hint
        assert "📭 已忽略 1 张非报销票据" in text
        assert "¥1.00" in text
        assert "加 未知发件人" not in text
        assert "主要来自" not in text
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tests/test_postprocess.py::TestPrintOpenClawSummary -v -k "ignored_line"
```

Expected: 6 FAIL（`ignored_records` kwarg 不被 accept，或新输出格式不存在）。

- [ ] **Step 3: 改 `print_openclaw_summary` 签名 + IGNORED 行渲染**

打开 `scripts/postprocess.py`。

(a) 签名改动 — 定位到第 1116-1128 行 `def print_openclaw_summary(...)`，把 `ignored_count: int = 0,  # v5.7 Unit 4` 那一行替换为：

```python
    ignored_records=None,  # v5.8 Unit B (replaces ignored_count)
```

(b) docstring 末尾（约第 1138-1140 行，在 `Raises ValueError ...` 之后）追加一行：

```
    ``ignored_records`` is the list of records that got IGNORED_ prefix
    filenames. When non-empty, emits a 2-line 📭 block: totals-by-currency
    + top sender + reply-to-add-exclusion CTA. None or [] emits nothing.
```

(c) 替换当前第 1229-1234 行的 IGNORED 渲染块：

原块：
```python
    # 9.5. v5.7 Unit 4: IGNORED count line (only if any were filtered)
    if ignored_count:
        writer(
            f"📭 已忽略 {ignored_count} 张非报销票据"
            f"（详见下载报告.md §已忽略的非报销票据）"
        )
```

换为：
```python
    # 9.5. v5.8 Unit B: IGNORED summary — totals-by-currency + top sender + CTA.
    # If all records lack a parseable sender domain, suppress the CTA (a reply
    # of "加 未知发件人" would be useless) and collapse to one line.
    if ignored_records:
        totals, top_domain, _top_count = _ignored_summary(ignored_records)
        total_str = " / ".join(
            f"{currency_symbol(cur)}{amt:.2f}"
            for cur, amt in sorted(totals.items())
        )
        if top_domain != "未知发件人":
            writer(
                f"📭 已忽略 {len(ignored_records)} 张非报销票据"
                f"（{total_str}，主要来自 {top_domain}）"
            )
            writer(
                f'   加过滤规则？回复 "加 {top_domain}" '
                f"我帮你写进 learned_exclusions.json"
            )
        else:
            writer(
                f"📭 已忽略 {len(ignored_records)} 张非报销票据"
                f"（{total_str}）"
            )
```

- [ ] **Step 4: 改两个调用点**

打开 `scripts/download-invoices.py`。

(a) 第一个调用点（约 1091-1102 行，`_run_postprocess_only` 里）。把 `ignored_count=len(ignored_records),` 一行改成：

```python
        ignored_records=ignored_records,
```

(b) 第二个调用点（约 1523-1534 行，`main()` 里）。同样把 `ignored_count=len(ignored_records),` 改成 `ignored_records=ignored_records,`。

- [ ] **Step 5: 跑 B2 测试**

```bash
python3 -m pytest tests/test_postprocess.py::TestPrintOpenClawSummary -v
```

Expected: 新 6 + 既有 `TestPrintOpenClawSummary` 测试全绿。若既有测试里还有别的用了 `ignored_count=` kwarg 的调用，它们必红——逐个改成 `ignored_records=`。用 `grep -n "ignored_count=" tests/test_postprocess.py` 核一遍。

- [ ] **Step 6: 跑全套测试确认无回归**

```bash
python3 -m pytest tests/ -q
```

Expected: 全绿。

- [ ] **Step 7: Commit**

```bash
git add scripts/postprocess.py scripts/download-invoices.py tests/test_postprocess.py
git commit -m "$(cat <<'EOF'
feat(report): IGNORED summary line with totals + top sender CTA (v5.8 Unit B)

Replace the one-liner "📭 已忽略 N 张（详见下载报告.md）" with a 2-line
block: total-by-currency + top sender domain, plus a reply CTA that
tells the user exactly what to say to register an exclusion rule.
Signature change: ignored_count: int → ignored_records: list[dict] |
None. When all records lack a sender domain, collapse to one line and
suppress the CTA (a reply of "加 未知发件人" is useless).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Unit C — `run_supplemental` 静默分支 + SKILL.md 合约

## Task C1: `main()` 初运行 `run_supplemental` 静默分支

**Files:**
- Modify: `scripts/download-invoices.py:1520-1534`（`main()` 里 `print_openclaw_summary` 调用前加静默分支）
- Test: `tests/test_agent_contract.py`（新增 `TestRunSupplementalSilence`）

- [ ] **Step 1: 写失败测试 — 初运行 + run_supplemental → 无哨兵 + stderr AGENT_HINT + exit 5**

在 `tests/test_agent_contract.py` 文件末尾加：

```python
class TestRunSupplementalSilence:
    """v5.8 Unit C: initial run deferring to supplemental emits no
    user-facing output (no CHAT_MESSAGE_*, no CHAT_ATTACHMENTS), only a
    stderr AGENT_HINT and exit 5. Supplemental runs still emit sentinels
    regardless of status (prevents user silence on max-iterations)."""

    @pytest.fixture
    def cli_module(self):
        import importlib.util, pathlib
        root = pathlib.Path(__file__).parent.parent
        spec = importlib.util.spec_from_file_location(
            "dl_cli_silence",
            root / "scripts" / "download-invoices.py",
        )
        mod = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(mod)
        return mod

    def _write_missing_json(self, output_dir, recommended_next_action):
        import json, os, pathlib
        pathlib.Path(output_dir).mkdir(parents=True, exist_ok=True)
        payload = {
            "schema_version": "1.0",
            "iteration": 1,
            "iteration_cap": 3,
            "status": "needs_retry" if recommended_next_action == "run_supplemental" else "converged",
            "recommended_next_action": recommended_next_action,
            "convergence_hash": "0" * 16,
            "items": [],
            "out_of_range_items": [],
        }
        with open(os.path.join(output_dir, "missing.json"), "w") as f:
            json.dump(payload, f)
        return payload

    def test_initial_run_with_run_supplemental_emits_agent_hint_no_sentinels(
        self, tmp_path, cli_module, monkeypatch, capsys,
    ):
        """End-to-end: initial run whose missing.json says run_supplemental
        must exit 5 + stderr AGENT_HINT + NO CHAT_MESSAGE_START/END
        anywhere in stdout. Delegate-to-agent contract."""
        # Stub the Gmail + LLM + matching layers so main() runs without
        # hitting network or creds; force recommended_next_action=run_supplemental.
        # The simplest path: stub write_missing_json to return a prebaked payload.
        prebaked = self._write_missing_json(str(tmp_path), "run_supplemental")
        monkeypatch.setattr(
            cli_module, "write_missing_json",
            lambda path, **kw: prebaked,
        )
        # Minimal stubs for the rest of the pipeline.
        monkeypatch.setattr(cli_module, "preflight_checks", lambda *a, **kw: 0)
        monkeypatch.setattr(cli_module, "GmailClient", lambda *a, **kw: _StubGmail())
        monkeypatch.setattr(cli_module, "_do_search_and_classify",
                            lambda *a, **kw: [])
        monkeypatch.setattr(cli_module, "_do_download_round",
                            lambda *a, **kw: ([], [], []))
        monkeypatch.setattr(cli_module, "analyze_pdf_batch",
                            lambda *a, **kw: None)
        monkeypatch.setattr(cli_module, "rename_by_ocr",
                            lambda recs, **kw: None)
        monkeypatch.setattr(cli_module, "do_all_matching",
                            lambda recs: {"hotel": {}, "ridehailing": {},
                                          "unparsed": [], "dedup_removed": []})
        monkeypatch.setattr(cli_module, "build_aggregation",
                            lambda m, r: {"voucher_count": 0, "grand_total": 0.0,
                                           "rows": [], "subtotals": {},
                                           "unmatched": {"hotel_invoices": 0,
                                                          "hotel_folios": 0,
                                                          "rh_invoices": 0,
                                                          "rh_receipts": 0},
                                           "low_conf": {"count": 0, "amount": 0.0}})
        monkeypatch.setattr(cli_module, "write_report_md", lambda *a, **kw: None)
        monkeypatch.setattr(cli_module, "write_summary_csv", lambda *a, **kw: 0)
        monkeypatch.setattr(cli_module, "zip_output", lambda *a, **kw: str(tmp_path / "z.zip"))

        # Run main() with argv set for initial (no --supplemental).
        import sys as _sys
        old_argv = _sys.argv[:]
        _sys.argv = [
            "download-invoices.py",
            "--start", "2026/01/01", "--end", "2026/04/01",
            "--output", str(tmp_path),
        ]
        try:
            with pytest.raises(SystemExit) as excinfo:
                cli_module.main()
        finally:
            _sys.argv = old_argv

        captured = capsys.readouterr()
        assert excinfo.value.code == 5, f"expected exit 5, got {excinfo.value.code}"
        assert "AGENT_HINT: run_supplemental" in captured.err
        assert "CHAT_MESSAGE_START" not in captured.out
        assert "CHAT_MESSAGE_END" not in captured.out
        assert "CHAT_ATTACHMENTS:" not in captured.out

    def test_supplemental_run_still_emits_sentinels(
        self, tmp_path, cli_module, monkeypatch, capsys,
    ):
        """Supplemental run (--supplemental) with run_supplemental status
        still emits CHAT_MESSAGE_START/END and CHAT_ATTACHMENTS — prevents
        user silence when the supplemental loop hits max_iterations."""
        prebaked = self._write_missing_json(str(tmp_path), "run_supplemental")
        monkeypatch.setattr(
            cli_module, "write_missing_json",
            lambda path, **kw: prebaked,
        )
        # (same stubs as above; reuse via inline copy to keep tests independent)
        monkeypatch.setattr(cli_module, "preflight_checks", lambda *a, **kw: 0)
        monkeypatch.setattr(cli_module, "GmailClient", lambda *a, **kw: _StubGmail())
        monkeypatch.setattr(cli_module, "_do_search_and_classify",
                            lambda *a, **kw: [])
        monkeypatch.setattr(cli_module, "_do_download_round",
                            lambda *a, **kw: ([], [], []))
        monkeypatch.setattr(cli_module, "analyze_pdf_batch",
                            lambda *a, **kw: None)
        monkeypatch.setattr(cli_module, "rename_by_ocr",
                            lambda recs, **kw: None)
        monkeypatch.setattr(cli_module, "do_all_matching",
                            lambda recs: {"hotel": {}, "ridehailing": {},
                                          "unparsed": [], "dedup_removed": []})
        monkeypatch.setattr(cli_module, "build_aggregation",
                            lambda m, r: {"voucher_count": 0, "grand_total": 0.0,
                                           "rows": [], "subtotals": {},
                                           "unmatched": {"hotel_invoices": 0,
                                                          "hotel_folios": 0,
                                                          "rh_invoices": 0,
                                                          "rh_receipts": 0},
                                           "low_conf": {"count": 0, "amount": 0.0}})
        monkeypatch.setattr(cli_module, "write_report_md", lambda *a, **kw: None)
        monkeypatch.setattr(cli_module, "write_summary_csv", lambda *a, **kw: 0)
        monkeypatch.setattr(cli_module, "zip_output", lambda *a, **kw: str(tmp_path / "z.zip"))
        monkeypatch.setattr(cli_module, "merge_supplemental_downloads",
                            lambda *a, **kw: [])

        import sys as _sys
        old_argv = _sys.argv[:]
        _sys.argv = [
            "download-invoices.py", "--supplemental",
            "--start", "2026/01/01", "--end", "2026/04/01",
            "--output", str(tmp_path),
        ]
        try:
            with pytest.raises(SystemExit):
                cli_module.main()
        finally:
            _sys.argv = old_argv

        captured = capsys.readouterr()
        assert "CHAT_MESSAGE_START" in captured.out
        assert "CHAT_MESSAGE_END" in captured.out
        assert "AGENT_HINT: run_supplemental" not in captured.err


# Minimal stub for GmailClient so main() can proceed without creds.
class _StubGmail:
    def __init__(self, *a, **kw): pass
    def search(self, *a, **kw): return []
    def list_messages(self, *a, **kw): return []
    def get_message(self, *a, **kw): return {}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
python3 -m pytest tests/test_agent_contract.py::TestRunSupplementalSilence -v
```

Expected: 1 FAIL (initial + run_supplemental 仍然发哨兵 + AGENT_HINT 不存在)，1 PASS（supplemental run 本来就发哨兵）。

- [ ] **Step 3: 在 `main()` 加静默分支**

打开 `scripts/download-invoices.py`。定位到第 1520-1534 行，当前结构是：

```python
    # --- Step 11: OpenClaw chat summary (stdout + run.log). MUST be called
    #     before log.close() — writer=say dual-writes to the still-open log.
    say("")
    print_openclaw_summary(
        aggregation,
        output_dir=output_dir,
        zip_path=zip_path,
        csv_path=csv_path,
        md_path=report_path,
        log_path=log_path,
        missing_status=missing_payload["recommended_next_action"],
        date_range=(args.start, args.end),
        writer=say,
        ignored_records=ignored_records,
    )
```

把 `say("")` 之后、`print_openclaw_summary(...)` 之前插入一段新块：

```python
    # --- v5.8 Unit C: silent run_supplemental deferral ---
    # Initial run (not --supplemental) with recommended_next_action ==
    # "run_supplemental" emits NO user-facing output. The Agent reads the
    # AGENT_HINT from stderr, runs the supplemental command, and only THAT
    # run's print_openclaw_summary delivers CHAT_MESSAGE_*/CHAT_ATTACHMENTS
    # to the user. Prevents the user from receiving two zips (an incomplete
    # one now + a complete one after supplemental) whose CSV/MD/zip contents
    # differ.
    if (not args.supplemental
            and missing_payload["recommended_next_action"] == "run_supplemental"):
        sys.stderr.write(
            f"AGENT_HINT: run_supplemental "
            f"--start {args.start} --end {args.end} "
            f"--output {shlex.quote(os.path.abspath(args.output))}\n"
        )
        log.close()
        sys.exit(EXIT_PARTIAL)
```

**Verify imports at top of file include `shlex` and `sys`** — `sys` is already imported (used elsewhere). `shlex` is used at postprocess.py:1225 indirectly; confirm download-invoices.py has `import shlex` near the top. Run:

```bash
grep -n "^import shlex\|^from shlex" /Users/kentpeng/projects/gmail-invoice-downloader/scripts/download-invoices.py
```

If not present, add `import shlex` near the existing imports.

- [ ] **Step 4: 跑 C1 测试确认通过**

```bash
python3 -m pytest tests/test_agent_contract.py::TestRunSupplementalSilence -v
```

Expected: 2 PASS.

- [ ] **Step 5: 跑现有哨兵合约测试（可能需要松绑）**

```bash
python3 -m pytest tests/test_agent_contract.py::TestChatSentinelContract -v 2>&1 | head -40
```

如果任何既有测试断言 "initial run with needs_retry always emits CHAT_MESSAGE_START"，把它们改为更精确的前置条件："unless recommended_next_action == 'run_supplemental'"。用 `grep -n "CHAT_MESSAGE_START" tests/test_agent_contract.py` 检查。

预期大多数哨兵测试用的是 `missing_status=stop`，不会被静默分支影响，应继续绿。如果有一个显式测"needs_retry 走哨兵路径"的，改 fixture 让 `recommended_next_action="ask_user"`（还在 needs_retry 的 umbrella 下但不触发静默）或按上面的逻辑松绑。

- [ ] **Step 6: 跑全套测试确认无回归**

```bash
python3 -m pytest tests/ -q
```

Expected: 全绿。

- [ ] **Step 7: Commit**

```bash
git add scripts/download-invoices.py tests/test_agent_contract.py
git commit -m "$(cat <<'EOF'
feat(contract): silent run_supplemental deferral (v5.8 Unit C.1)

Initial runs (not --supplemental) whose missing.json recommends
run_supplemental now exit 5 with "AGENT_HINT: run_supplemental ..." on
stderr and emit NO CHAT_MESSAGE_START/END or CHAT_ATTACHMENTS. The
wrapping Agent runs the supplemental command; only that run's summary
goes to the user. Prevents users from receiving an intermediate zip
whose contents change after the supplemental merge.

Supplemental runs still emit sentinels regardless of status (guards
against user silence on max_iterations_reached).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task C2: SKILL.md 合约更新

**Files:**
- Modify: `SKILL.md`（6 节 + 1 新增）

- [ ] **Step 1: Read current SKILL.md sections**

```bash
grep -n "^## \|^### " /Users/kentpeng/projects/gmail-invoice-downloader/SKILL.md | head -40
```

Locate: `## Exit Codes` (~line 510), `## Presenting Results to the User` (~line 526), `## Loop Playbook` (~line 389), `## Lessons Learned` (~line 650+).

- [ ] **Step 2: 扩 §Exit Codes 表的 exit=5 注解**

打开 `SKILL.md`。定位到 exit=5 那行表格（约第 520 行：`| 5 | 部分成功 | 正常出交付物...`）。把它的说明列换成：

```
| 5 | 部分成功 / 补搜延迟 | 两种子语义：(a) 正常出交付物但有 UNPARSED / failed 项 → 查 missing.json 按常规流程；(b) **stderr 含 `AGENT_HINT: run_supplemental ...`** → stdout 为空（无 CHAT_MESSAGE_START/END、无 CHAT_ATTACHMENTS）；**不要**向用户转发任何东西，直接按 stderr 指令跑补搜（v5.8 Unit C） |
```

- [ ] **Step 3: §Presenting Results → Agent Playbook 加守则 0**

定位到 `### Agent Playbook` (约第 561 行)。把 "After each Skill run:" 下的步骤列表**前**插入一条新守则：

```markdown
0. **Check for silent-deferral first (v5.8):** If stdout does NOT contain
   `CHAT_MESSAGE_START` (for example, exit=5 with stderr `AGENT_HINT: run_supplemental ...`),
   skip all subsequent steps. Do not send anything to the user.
   Execute the command in the `AGENT_HINT:` line instead, then apply this
   playbook to that supplemental run's output.
```

- [ ] **Step 4: §Invariants 松绑**

定位到 `### Invariants` (约第 582 行)。把
```
- Each sentinel appears at most **once** per Skill run.
```
改成：
```
- Each sentinel appears **at most once** per Skill run. **May also appear zero times** — on early-error paths (exit 2/3/4) and on the v5.8 `run_supplemental` deferral path (exit 5 + stderr `AGENT_HINT:`). Agents MUST check for sentinel presence before forwarding; absence is a valid state.
```

- [ ] **Step 5: §Loop Playbook 加一句**

定位到 `## Loop Playbook（Agent 使用）` (约第 389 行)。在其任意合适位置（通常是 needs_retry 相关段落之后）加一句（改写或追加）：

```markdown
**v5.8 note:** The initial run never delivers intermediate results to the user. If missing.json says `recommended_next_action: run_supplemental`, the Skill emits only `AGENT_HINT:` on stderr and exits 5 — no chat message, no attachments. Agents should forward a brief "正在补搜..." placeholder to the user before executing the supplemental command to avoid long silences. Only the supplemental run's `CHAT_MESSAGE_START/END` + `CHAT_ATTACHMENTS:` block reaches the user.
```

- [ ] **Step 6: 新增 §User Reply Conventions**

在 `## Loop Playbook` 或 `## Presenting Results` 之后，加一节：

```markdown
## User Reply Conventions

Some Skill messages invite the user to reply with a short command. The Agent, not the Skill, is responsible for acting on these replies.

### "加 `<domain>`" — register an exclusion rule

When the Skill summary contains a line like:

```
   加过滤规则？回复 "加 <domain>" 我帮你写进 learned_exclusions.json
```

and the user replies with `加 <domain>`:

1. Open `<output_dir>/../learned_exclusions.json` (the same file the Skill reads at start; typically at repo root or next to the output dir).
2. Append a new entry to the `senders: []` array:
   ```json
   {"domain": "<domain>", "reason": "user approved", "added_at": "<ISO-8601 UTC>"}
   ```
3. Confirm to the user: `已加 <domain>，下次跑该时间窗会自动过滤`.

The Skill itself does NOT mutate `learned_exclusions.json`; this is intentional (keeps the Skill deterministic and replayable).
```

- [ ] **Step 7: §Lessons Learned 新增 `🟢 v5.8`**

定位到 `## Lessons Learned` 节。在 `### 🟢 v5.7` 之后（保持时间倒序：最新在最上），**插在 v5.7 之前**（倒序首位）。

```markdown
### 🟢 v5.8 — currency-aware IGNORED summary + silent run_supplemental

**动机**：2025Q1 批次复盘发现三个独立但互相牵扯的问题：(a) `下载报告.md` §已忽略 把 USD Anthropic 账单显示成 `¥10.00` —— OCR prompt 从未定义 `currency`，MD 渲染代码读一个 LLM 不产出的字段，fallback 永远 `¥`。(b) 摘要里 `📭 已忽略 N 张...（详见 MD）` 信息密度太低，用户不翻 MD → add-exclusion 闭环断。(c) `status=needs_retry` 时 Skill 先发一套完整摘要 + 中间 zip 给用户，Agent 再跑补搜后又发一套最终的 —— 用户收到 2 份作废交付物（CSV/MD/zip 均会变）。

- **feat(prompts):** `scripts/core/prompts.py` 通用字段表新增 `currency` 行（ISO-4217 三字母码，默认 CNY）；3 段 example JSON（电子发票 / 酒店水单 / 网约车）各加 `"currency": "CNY"` 锚定 LLM 输出。
- **feat(llm_ocr):** `extract_from_bytes` 出口对缺失的 `currency` 字段 defensive fill `"CNY"`，兼容老 cache（cache key = `sha256(pdf_bytes)`，不含 prompt，prompt 变动不失效 cache）。
- **feat(postprocess):** 顶层 `_CURRENCY_SYMBOLS` + `currency_symbol()` 查表（CNY→¥ / USD→$ / EUR→€ / GBP→£ / JPY→¥ / HKD→HK$；未知码 `"{CODE} "` fallback）。`print_openclaw_summary` 参数 `ignored_count: int` → `ignored_records: list[dict] | None`；新增 `_ignored_summary()` helper；IGNORED 行换成 2 行"总金额 + 主 sender + 回复 CTA"。多币种用 `/` 分隔、字母序。`top_domain == "未知发件人"` 时省 CTA 行。
- **feat(report):** `write_report_md` §已忽略 渲染从硬编 `¥` 前缀切 `currency_symbol()`，USD 账单现在正确显示 `$10.00`。
- **feat(contract):** `main()` 两个 `print_openclaw_summary` 调用前加守门分支 —— `not args.supplemental and missing_status == "run_supplemental"` → `sys.stderr.write("AGENT_HINT: run_supplemental ...")` + `sys.exit(EXIT_PARTIAL)`。不发哨兵不发附件。补搜这次仍发哨兵（防 max_iterations 用户沉默）。exit 5 复用，不加 exit 6 —— 子语义差异由 stderr 哨兵表达。
- **docs(SKILL):** §Exit Codes exit=5 行注解扩充；§Agent Playbook 加守则 0（sentinel absence is valid）；§Invariants 松绑成 "at most once, may be zero"；§Loop Playbook 追加 v5.8 note；新增 §User Reply Conventions 规范 "加 `<domain>`" 回复落地。

### Agent 合约变化

- **`missing.json` schema 不动**（仍 1.0，不加 `ignored_count` 顶层）。
- **`CHAT_MESSAGE_*` / `CHAT_ATTACHMENTS:` 可以为零**（新增情境：初运行 `run_supplemental` 延迟）。Agents 必须先检查 sentinel 存在性再决定是否转发。
- **新 stderr 哨兵 `AGENT_HINT: run_supplemental ...`**：出现时 Skill stdout 为空，Agent 按该行指令继续，不向用户转发任何东西。
- **新用户回复约定**：`加 <domain>` → Agent 写 `learned_exclusions.json`，Skill 不碰。

### 不要再做

- **不要** 在 §IGNORED 渲染里硬编 `¥` 前缀——永远通过 `currency_symbol()` 查表。未来加新币种（KRW/TWD/AUD/...）只需改 `_CURRENCY_SYMBOLS` dict。
- **不要** 在 `_ignored_summary` 里按金额排序主 sender —— "主要来自" 是按记录条数，不是按金额（同金额大小的 10 张 vs 1 张，大部分用户直觉选 10 张）。
- **不要** 为 `run_supplemental` 静默分支新建 exit code 6。现有 exit 5 + stderr `AGENT_HINT:` 区分子语义已经够；新 exit code 会破坏 Agent 已有的 exit 表匹配逻辑。
- **不要** 在 Skill 里加 "占位消息" 逻辑（"正在补搜..."）。静默期间的 UX 兜底属 Agent 职责，SKILL.md §Loop Playbook 建议但不强制。
- **不要** 让 Skill 自动写 `learned_exclusions.json`。保持 Skill 纯函数语义，可重放；用户决策面的文件只归 Agent。
```

- [ ] **Step 8: SKILL.md line 1 版本号**

打开 `SKILL.md`。第 1 行目前应当是 `# Gmail 发票下载器 (v5.7.1)` 或类似。改为 `# Gmail 发票下载器 (v5.8)`。

运行以验证：
```bash
head -1 /Users/kentpeng/projects/gmail-invoice-downloader/SKILL.md
```

- [ ] **Step 9: 跑 doctor / agent-contract 测试做 sanity check**

```bash
python3 -m pytest tests/test_agent_contract.py -q
```

Expected: 全绿（C1 已经处理哨兵测试松绑；C2 只改 SKILL.md 文字）。

- [ ] **Step 10: Commit**

```bash
git add SKILL.md
git commit -m "$(cat <<'EOF'
docs(SKILL): v5.8 contract — silent run_supplemental + user reply conventions (v5.8 Unit C.2)

Update 4 existing sections (§Exit Codes, §Presenting Results, §Invariants,
§Loop Playbook) + add §User Reply Conventions + §Lessons Learned v5.8.

Contract changes:
- CHAT_MESSAGE_*/CHAT_ATTACHMENTS may appear zero times (silent deferral)
- New stderr sentinel: AGENT_HINT: run_supplemental ...
- User reply "加 <domain>" → Agent writes learned_exclusions.json

Bump SKILL.md line 1 → v5.8.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Unit D — 收尾：CHANGELOG

## Task D1: CHANGELOG

**Files:**
- Modify: `CHANGELOG.md`（最上面新增 `## v5.8` 条目）

- [ ] **Step 1: 写 CHANGELOG 新条目**

打开 `CHANGELOG.md`。在第 `## v5.7.1` 条目**前面**插入：

```markdown
## v5.8 — 货币感知的 IGNORED 摘要 + run_supplemental 静默分支 (2026-05-03)

**动机**：2025Q1 批次复盘三个独立但互相牵扯的问题：USD Anthropic 账单在 §IGNORED 显示 `¥10.00`；摘要里 `📭 已忽略 N 张（详见 MD）` 信息太少用户不翻 MD 看不到 sender + CTA；`status=needs_retry` 时 Skill 发一次中间 zip，Agent 跑补搜后又发一次最终 zip，用户收到 2 份交付物前者作废（CSV/MD/zip 均会变）。

- **feat(prompts):** OCR 通用字段表新增 `currency` 字段（ISO-4217，默认 CNY）；3 段 example JSON 各加 `"currency": "CNY"` 锚定 LLM 输出（Unit A.1）。
- **feat(llm_ocr):** `extract_from_bytes` 出口 defensive fill `currency=CNY`，含老 cache 命中路径（Unit A.2）。cache key 不含 prompt，老 cache 仍命中；`"currency"` 字段由出口兜底保证存在。
- **feat(postprocess):** 顶层加 `_CURRENCY_SYMBOLS` + `currency_symbol()` 查表（CNY/USD/EUR/GBP/JPY/HKD，未知码 `"{CODE} "` fallback）（Unit A.3）。`_ignored_summary()` helper 聚合 IGNORED 记录成 `(totals_by_currency, top_domain, top_count)`（Unit B.1）。`print_openclaw_summary` 参数 `ignored_count: int` → `ignored_records: list[dict] | None`；摘要 IGNORED 行换成 2 行"总金额 + 主 sender + 回复 CTA"，多币种 `/` 分隔字母序，`top_domain="未知发件人"` 时省 CTA（Unit B.2）。
- **feat(report):** `write_report_md` §已忽略 渲染从硬编 `¥` 切 `currency_symbol()`，USD 账单现在正确显示 `$10.00`（Unit A.4）。
- **feat(contract):** `main()` 新增守门分支 —— 非 `--supplemental` 初运行 + `recommended_next_action=="run_supplemental"` → stderr 打 `AGENT_HINT: run_supplemental ...` + `sys.exit(EXIT_PARTIAL)`；跳过 `print_openclaw_summary`，不发哨兵不发附件。补搜那次仍发哨兵（防 max_iterations 用户沉默）。exit 5 复用，不加 exit 6 —— 子语义由 stderr 哨兵表达（Unit C.1）。
- **docs(SKILL):** 更新 4 节（§Exit Codes / §Presenting Results / §Invariants / §Loop Playbook）+ 新增 §User Reply Conventions + §Lessons Learned v5.8（Unit C.2）。
- **test(suite):** 全绿；新增 `TestCurrencyPromptContract` / `TestCurrencyDefensiveFill` / `TestCurrencySymbolTable` / `TestIgnoredSummaryHelper` / `TestIgnoredMdRenderingCurrency` / `TestPrintOpenClawSummary`（扩 IGNORED 行）/ `TestRunSupplementalSilence`。

### Agent 合约变化

- **`missing.json` schema 不动**（仍 `"1.0"`，不加 `ignored_count` 顶层字段）。
- **`CHAT_MESSAGE_*` / `CHAT_ATTACHMENTS:` 可以为零**。初运行 `run_supplemental` 延迟路径下 Skill stdout 为空，Agent 必须先检查 sentinel 存在性再决定是否转发。
- **新 stderr 哨兵 `AGENT_HINT: run_supplemental ...`**。出现时 Agent 不转发任何东西，按该行指令跑补搜。
- **新用户回复约定 `加 <domain>`**：Agent 在 `learned_exclusions.json` 追加 `senders[]` 项；Skill 不碰。

### 不受影响

- `scripts/core/matching.py` / `scripts/core/classify.py` / `scripts/invoice_helpers.py` 不动。
- reimbursable 发票（酒店 / 餐饮 / 网约车）仍然全 `¥`，aggregation / CSV / 主 zip 结构不变。
- 既有 `zip_output` / `rename_by_ocr` / `build_aggregation` / `missing.json` writer 行为不变。

### 升级备注

- 开发阶段单用户，不主动清 `~/.cache/gmail-invoice-downloader/ocr/`。老 cache 自动被 defensive fill 兜底成 `currency=CNY`，不会崩溃；只有 pre-v5.8 的 IGNORED Anthropic cache 命中时会继续显示 `¥`。下次同一 PDF 重新跑（或手工 `rm` 对应 cache 条目）即正确。
```

- [ ] **Step 2: 跑一次全套测试做发布前核查**

```bash
python3 -m pytest tests/ -q
```

Expected: 全绿。若红，停下排查并修。

- [ ] **Step 3: 手工 smoke — 跑一次真实初运行 + 补搜（可选但推荐）**

用真实 Gmail + LLM 数据跑端到端（SKILL.md §references/seasonal-smoke.md 有正常 smoke runbook）。重点核对：

1. 初运行如果 `recommended_next_action=run_supplemental`：
   - stdout 为空（用 `tee` 捕捉也看不到哨兵）；
   - stderr 最后一行是 `AGENT_HINT: run_supplemental --start ... --end ... --output ...`；
   - exit 5。
2. 跑 stderr 里那条补搜命令；
3. 补搜 stdout 里 `CHAT_MESSAGE_START` 之前是正常日志，之间是 `📄 发票报销包 ...`，结尾有 `📭 已忽略 N 张...（$USD总额，主要来自 mail.anthropic.com）` + `加 mail.anthropic.com ...` 两行；`CHAT_ATTACHMENTS:` 声明 zip/md/csv。
4. `下载报告.md` §已忽略的非报销票据 里 USD 项是 `$10.00` 而不是 `¥10.00`。

如果任何一条不符，停下排查。smoke 非硬门槛（开发阶段单用户可跳），但落地后期望手跑一次。

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md
git commit -m "$(cat <<'EOF'
chore(v5.8): bump version + CHANGELOG (v5.8 Unit D)

v5.8 release summary:
- feat(prompts): currency field in OCR schema
- feat(llm_ocr): defensive currency=CNY fill
- feat(postprocess): currency_symbol lookup + _ignored_summary helper
- feat(report): IGNORED summary line + CTA; MD §IGNORED uses currency_symbol
- feat(contract): silent run_supplemental deferral + AGENT_HINT stderr
- docs(SKILL): contract updates + User Reply Conventions

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 5: Push branch (no merge yet; PR manually later)**

```bash
cd /Users/kentpeng/projects/gmail-invoice-downloader
git push --no-verify -u origin feat/v5.8-currency-summary-silent-retry
```

---

## Post-implementation checks

- [ ] `python3 scripts/doctor.py` exits 0.
- [ ] `python3 -m pytest tests/ -q` 全绿。
- [ ] `head -1 SKILL.md` = `# Gmail 发票下载器 (v5.8)` 或 `# Gmail Invoice Downloader (v5.8)` (whichever the existing convention is).
- [ ] `grep -c '"currency": "CNY"' scripts/core/prompts.py` ≥ 3。
- [ ] `grep -n "currency_symbol\|_ignored_summary" scripts/postprocess.py` 列出至少 4 次引用。
- [ ] `grep -n "AGENT_HINT: run_supplemental" scripts/download-invoices.py` 找到 1 次（静默分支）。
- [ ] Manual smoke（Task D1 Step 3）如果跑了，核对通过。
