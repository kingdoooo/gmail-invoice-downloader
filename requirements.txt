# gmail-invoice-downloader v5.3 dependencies

# Default LLM provider (AWS Bedrock via IAM role / profile / AKSK / API Key)
boto3>=1.35.17   # 1.35.17+ reads AWS_BEARER_TOKEN_BEDROCK

# Optional LLM providers — install what you use
# For LLM_PROVIDER=anthropic or anthropic-compatible
# anthropic>=0.34
# For LLM_PROVIDER=openai or openai-compatible
# openai>=1.50

# Gmail API client (used by scripts/invoice_helpers.py GmailClient)
# Note: Gmail uses raw HTTPS + token refresh logic hand-rolled in GmailClient,
# so google-api-python-client is NOT required; token.json is read directly.

# Test-only
# pytest>=8.0
